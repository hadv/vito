{
  "name": "vim-safe-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"cd client && npm run dev\" \"cd server && npm run start:dev\"",
    "install": "cd client && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.5",
    "concurrently": "^8.2.2"
  }
}
